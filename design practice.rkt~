;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |design practice|) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
; String -> String
; extracts the first letter of a string
; given: "hello world" expect: "h"
; given: "h" expect: "h"
(define (string-first string) (string-ith string 0))

; String -> String
; extracts the last letter of a string
(define (string-last string) "B" )
